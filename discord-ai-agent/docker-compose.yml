services:
  bot:
    build: .
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/bot.db
    volumes:
      # Named volume (not a host bind mount): portable and not deleted with the repo.
      - bot_data:/app/data
    restart: unless-stopped

volumes:
  bot_data:
