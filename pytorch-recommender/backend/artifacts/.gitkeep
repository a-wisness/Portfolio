# Trained model artifacts (model.pt) live here. A committed checkpoint in this
# directory is baked into the Docker image and served immediately. If absent,
# the API reports model_loaded: false until you run `python -m app.train`.
