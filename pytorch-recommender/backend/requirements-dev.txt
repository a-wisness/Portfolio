# Install on top of requirements.txt to run the test suite:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
httpx>=0.27          # required by FastAPI's TestClient
matplotlib>=3.8      # benchmark plots (research/dev only — not in the serving image)
